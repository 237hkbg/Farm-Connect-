import React from "react";

const SiteSettings: React.FC = () => {
  return (
    <div style={{ padding: "40px", textAlign: "center" }}>
      <h1>Site Settings</h1>
      <p>Admin can change site configurations here.</p>
    </div>
  );
};

export default SiteSettings;
